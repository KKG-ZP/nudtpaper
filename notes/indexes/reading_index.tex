\documentclass[UTF8,fontset=fandol,zihao=-4]{ctexart}

\input{../templates/preamble.tex}

\title{论文阅读总索引\\\large Reading Index}
\author{Notes Subsystem}
\date{最近维护日期：\today}

\newcommand{\paperfilelink}[1]{\href{run:#1}{\nolinkurl{#1}}}
\newcommand{\renderpaperentry}[6]{%
  \noindent
  \begin{tabularx}{\textwidth}{@{}lY@{}}
  \NoteField{标题}{#1}
  \NoteField{作者}{#2}
  \NoteField{阅读时间}{#3}
  \NoteField{一句话摘要}{#4}
  \NoteField{标签}{#5}
  \NoteField{笔记文件}{\paperfilelink{#6}}
  \end{tabularx}
  \vspace{0.4em}
  \hrule
  \vspace{0.9em}
}

\makeatletter
\newcommand{\paper@resetmeta}{%
  \gdef\paperindextitle{[缺失标题]}%
  \gdef\paperindexauthors{[缺失作者]}%
  \gdef\paperindexdate{[缺失阅读时间]}%
  \gdef\paperindexsummary{[缺失一句话摘要]}%
  \gdef\paperindexkeywords{[缺失标签]}%
}

\newcommand{\paper@loadmeta}[1]{%
  \paper@resetmeta
  \begingroup
  \def\paperindexmode{1}%
  \def\paperindexentry##1##2##3##4##5{%
    \xdef\paperindextitle{##1}%
    \xdef\paperindexauthors{##2}%
    \xdef\paperindexdate{##3}%
    \xdef\paperindexsummary{##4}%
    \xdef\paperindexkeywords{##5}%
  }%
  \input{../papers/#1.tex}%
  \endgroup
}

\newcommand{\paperentry}[1]{%
  \paper@loadmeta{#1}%
  \renderpaperentry
    {\paperindextitle}
    {\paperindexauthors}
    {\paperindexdate}
    {\paperindexsummary}
    {\paperindexkeywords}
    {../papers/#1.tex}%
}
\makeatother

\begin{document}

\maketitle

\section{使用方式}

这个索引文件用于快速浏览和维护“已读论文”的核心信息，不依赖目录自动扫描。后续新增论文时，只需要在这里追加一条 \verb|\paperentry{name}|，其中 \verb|name| 表示 `papers/` 目录下对应笔记文件去掉 `.tex` 之后的名字。

\begin{enumerate}
  \item 在 \verb|papers/| 中创建或复制笔记文件。
  \item 确保笔记头部已经定义好 \verb|\papertitle|、\verb|\paperauthors|、\verb|\readingdate|、\verb|\paperonesentence|、\verb|\paperkeywords|。
  \item 在 \verb|reading_index.tex| 中追加一条 \verb|\paperentry{name}| 记录。
\end{enumerate}

当前实现通过局部读取笔记文件中的头部元信息实现半自动索引，因此这些字段最好保持单行、简单文本，并保留模板默认的 \verb|\ifdefined\paperindexmode| 逻辑，不建议改成复杂宏结构。

\section{已收录条目}

\paperentry{sample_deepseekmath_note}

\paperentry{sample_note}

\end{document}
